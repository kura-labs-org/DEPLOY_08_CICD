pipeline {
    agent {
        label "agent"
    }
    stages {
        stage('Build') { 
            steps { 
                dir('application')
                sh '''
                   ls
                   pwd
                   '''
                sh 'echo "completed build"'
            }
        }
    }
}
